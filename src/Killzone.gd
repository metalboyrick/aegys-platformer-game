extends Area2D

func _physics_process(delta):
	#var playerHealth = get_node("Player").health
	#var touch = get_overlapping_bodies()
	#for body in touch:
		#f body.name == "Player" && !playerHealth == 0:
		#	get_tree().reload_current_scene()
		#else:
		#	get_tree().reload_current_scene()
			
	
	pass
